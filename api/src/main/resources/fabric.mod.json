{
    "schemaVersion": 1,
    "id": "floria-api",
    "version": "${version}",
    "name": "Floria API",
    "license": "GPL-3.0",
    "environment": "client",
    "depends": {
        "fabricloader": ">=${loader_version}",
        "fabric-language-kotlin": ">=${kotlin_loader_version}",
        "fabric-api": "*",
        "minecraft": "${minecraft_version}"
    }
}
